<% if !@domain.monitoring %>
  <%= button_to "Monitor",
                domain_path(@domain),
                method: :patch,
                params: { domain: { monitoring: true } },
                class: "bg-sky-500 text-white p-2 m-2 rounded" %>
<% else %>
  <%= button_to "Stop Monitoring",
                domain_path(@domain),
                method: :patch,
                params: { domain: { monitoring: false } },
                class: "bg-sky-500 text-white p-2 m-2 rounded" %>
<% end %>